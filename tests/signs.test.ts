
describe('getBackend', () => {
    it('should throw an error if tensorflow has not been set', () => {
        expect(1).toBe(1);
    });
});

describe('setBackend', () => {
    it('should set a tf singleton', () => {
        expect(1).toBe(1);
    });
});